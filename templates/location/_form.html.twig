{{ form_start(form) }}
    <div class="mb-3">
        {{ form_widget(form) }}
    </div>
    <button class="btn btn-galactic btn-lg w-100">{{ button_label|default('Valider la mission') }}</button>
{{ form_end(form) }}
