{{ form_start(form, {'attr': {'class': 'needs-validation'}}) }}
    <div class="row">
        <div class="col-md-12 mb-3">
            {{ form_widget(form) }}
        </div>
    </div>
    <div class="d-grid gap-2 d-md-flex justify-content-md-end">
        <button class="btn btn-primary px-4">{{ button_label|default('Enregistrer') }}</button>
    </div>
{{ form_end(form) }}
