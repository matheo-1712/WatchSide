{% extends 'base.html.twig' %}

{% block title %}Catalogue - WatchSide Galactic{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="d-flex justify-content-between align-items-center mb-5">
        <h1 class="text-hologram display-5 fw-bold">CATALOGUE GALACTIQUE</h1>
        {% if is_granted('ROLE_ADMIN') %}
            <a href="{{ path('app_film_new') }}" class="btn btn-outline-secondary btn-sm">
                <i class="bi bi-plus-lg"></i> Ajouter un film
            </a>
        {% endif %}
    </div>

    {# Filter/Search Placeholder #}
    <div class="glass-panel p-4 mb-5">
        <div class="row g-3 align-items-center">
            <div class="col-12">
                <div class="input-group">
                    <span class="input-group-text bg-transparent border-secondary text-light w-85">
                        <i class="bi bi-search"></i>
                    </span>
                    <input id="titreRechercher" type="text" class="form-control" placeholder="Rechercher un film..."
                           style="color: white;"
                           aria-label="Search">
                    {%  for film  in films %}

                        {{  film.titre ~ titreRechercher.value  }}
                        


                    {%  endfor %}
                </div>
            </div>
        </div>
    </div>
    <style>
        .form-control::placeholder {
            color: white !important;
            opacity: 1;
        }

        .form-control::-webkit-input-placeholder {
            color: white !important;
        }

        .form-control::-moz-placeholder {
            color: white !important;
            opacity: 1;
        }

        .form-control:-ms-input-placeholder {
            color: white !important;
        }

        .form-control::-ms-input-placeholder {
            color: white !important;
        }
    </style>

    {% if app.user and favoris is not empty %}
        <h2 class="mb-4">⭐ Mes favoris</h2>
        <div class="row g-4 mb-5">
            {% for favori in favoris %}
                <div class="col-sm-6 col-md-4 col-lg-3">
                    {% include 'film/_card.html.twig' with { film: favori.idFilm } %}
                </div>
            {% endfor %}
        </div>
        <br>
    {% endif %}

    <h2 class="mb-4"> Les films </h2>
    <div class="row g-4">
        {% for film in films %}
            <div class="col-sm-6 col-md-4 col-lg-3">
                {% include 'film/_card.html.twig' with { film: film } %}
            </div>
        {% endfor %}
    </div>

</div>
{% endblock %}
